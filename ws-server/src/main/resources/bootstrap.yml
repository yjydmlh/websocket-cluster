spring:
  application:
    #随机名字，做ws集群使用
    name: ${SpringApplicationName}
  #    name: ws
  cloud:
    nacos:
      server-addr: 192.168.137.47
      config:
        file-extension: yaml
  data:
    redis:
      host: 192.168.137.47
  threads:
    virtual:
      enabled: true
server:
  port: 8088

ws:
  path: /ws/{language}/{userId}/{device}
  port: 9090